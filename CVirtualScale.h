#pragma once
class CVirtualScale
{
public:

    // constructor / destructor
    CVirtualScale();
    ~CVirtualScale();

    // log the sent string
    void WriteMeasurment(CString csCommand);

};

